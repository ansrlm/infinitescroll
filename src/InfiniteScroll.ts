class InfiniteScroll {
  /**
   * @param {}
   */
  constructor(element: string, options: Record<string, unknown>) {
    console.log(element);
    console.log(options);
  }
}

export default InfiniteScroll;
